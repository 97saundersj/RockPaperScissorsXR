name: Unity Test & Build Action

on:
  workflow_dispatch:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
    
jobs:
  test:
    name: Test
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      # Checkout
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # Cache
      - uses: actions/cache@v3
        with:
          path: Library
          key: Library-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-
            
      # Debugging: Print secrets (DO NOT use in production)
      - name: Debug secrets
        run: |
          echo "UNITY_LICENSE: $(echo "${{ secrets.UNITY_LICENSE }}" | sed 's/./& /g')"
          echo "UNITY_EMAIL: $(echo "${{ secrets.UNITY_EMAIL }}" | sed 's/./& /g')"
          echo "UNITY_PASSWORD: $(echo "${{ secrets.UNITY_PASSWORD }}" | sed 's/./& /g')"

      # Test
      - name: Run tests
        uses: game-ci/unity-test-runner@v4
        env:
           UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
           UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
           UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
         githubToken: ${{ secrets.GITHUB_TOKEN }}

  build:
    name: Build
    permissions: write-all
    runs-on: windows-latest
    steps:
      # Checkout
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # Cache
      - uses: actions/cache@v3
        with:
          path: Library
          key: Library-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-
            
      # Debugging: Print secrets (DO NOT use in production)
      - name: Debug secrets
        run: |
          echo "UNITY_LICENSE: $(echo "${{ secrets.UNITY_LICENSE }}" | sed 's/./& /g')"
          echo "UNITY_EMAIL: $(echo "${{ secrets.UNITY_EMAIL }}" | sed 's/./& /g')"
          echo "UNITY_PASSWORD: $(echo "${{ secrets.UNITY_PASSWORD }}" | sed 's/./& /g')"

      # Build
      - name: Build project
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: Android # Build an Android .apk standalone app.

      # Output
      - uses: actions/upload-artifact@v3
        with:
          name: Build
          path: build
